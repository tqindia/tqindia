steps:
- block: Request release
  fields:
  - default: ALL
    key: region
    options:
    - label: us-west-1
      value: us-west-1
    - label: ALL
      value: ALL
    prompt: Please select a region for deployment
    select: Region
- command: buildkite-agent pipeline upload .buildkite/workflows/deploy-hell-$(buildkite-agent
    meta-data get region).yml
  label: Deploy hell environment
