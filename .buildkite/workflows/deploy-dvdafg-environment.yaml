steps:
- block: Request release
  fields:
  - default: ALL
    key: region
    options:
    - label: ALL
      value: ALL
    prompt: Please select a region for deployment
    select: Region
- command: buildkite-agent pipeline upload .buildkite/workflows/deploy-dvdafg-$(buildkite-agent
    meta-data get region).yml
  label: Deploy dvdafg environment
