steps:
- block: Request release
  fields:
  - default: ALL
    key: region
    options:
    - label: ap-southeast-4
      value: ap-southeast-4
    - label: ALL
      value: ALL
    prompt: Please select a region for deployment
    select: Region
- command: buildkite-agent pipeline upload .buildkite/workflows/deploy-production-devs-$(buildkite-agent
    meta-data get region).yml
  label: Deploy production-devs environment
