steps:
- block: Request release
  fields:
  - default: ALL
    key: region
    options:
    - label: us-east-1
      value: us-east-1
    - label: ALL
      value: ALL
    prompt: Please select a region for deployment
    select: Region
- command: buildkite-agent pipeline upload .buildkite/workflows/deploy-fdsafsadf-$(buildkite-agent
    meta-data get region).yml
  label: Deploy fdsafsadf environment
